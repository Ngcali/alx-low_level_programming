#include "main.h"
#include <stdio.h>

/**
 * print_number - prints an
 * integer.
 *
 * @n: integer to be printed.
 */

void print_number(int n)
{
	if (n <= 0 || n > 0)
	{
		_putchar(n + '0');
	}

	/*print_number(n + '0');*/
}
