#include "main.h"
#include <stdio.h>

char *_strcpy(char *dest, char *src)
{
	int i;
	my_counter;

	for (i = 0; src[i] != '\0'; i++)
	{}

	while (my_counter <= i)
	{
		src[my_counter] = dest[my_counter];
		my_counter;
	}

	return dest;
}
