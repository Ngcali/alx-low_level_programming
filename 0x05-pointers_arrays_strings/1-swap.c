#include "main.h"

/**
 * swap_int - swaps the values
 * of two integers.
 *
 * @*a: first integer to swap
 * with second integer.
 *
 * @*b: second integer to swap
 * with first integer.
 *
 * Return: void.
 */

void swap_int(int *a, int *b)
{
	int store_*a:

	*a = *b;
	*b = store_*a;
}
