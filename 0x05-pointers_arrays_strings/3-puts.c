#include "main.h"
#include <stdio.h>

/**
 * _puts - prints a string
 * followed by a new line.
 *
 * @*str: pointer which points
 * to str.
 *
 * @str: sting in which pointer
 * points to.
 */

void _puts(char *str)
{
	_puts(*str);
}
